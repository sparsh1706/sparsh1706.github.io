{
  "name": "personal-website",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/tailwind.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
